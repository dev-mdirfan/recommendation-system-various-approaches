
    # Data Processing
    # data = data.drop(['product_name', 'uniq_id', 'crawl_timestamp', 'product_url', 'product_category_tree', 'pid', 'retail_price', 'discounted_price', 'image', 'is_FK_Advantage_product', 'description', 'product_rating', 'overall_rating','brand', 'product_specifications',], axis=1)
    # search_data = tuple(zip(data['product_name'],data['uniq_id'],data['crawl_timestamp'],data['product_url'], data['product_category_tree'], data['pid'], data['retail_price'], data['discounted_price'], data['image'], data['is_FK_Advantage_product'], data['description'], data['product_rating'], data['overall_rating'], data['brand'], data['product_specifications']))



    
    # Combining data together
    # data = pd.concat([data1, data2], axis=1)

    
        # #combine data into tuple to show it to user
        # search_data = tuple(zip(data['Title'],data['ImageUrl'],data['Price'],data['Reviews']))